#include "main.h"

/**
 * set_string - sets value of a pointer to a char
 * @s: sets value of a pointer to pointer
 * @to: sets pointer to char
 */

void set_string(char **s, char *to)
{
	*s = to;
}

